# Neon(Serverless Postgres)连接串
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# JWT 签名密钥(请用强随机值,生产务必从密钥管理注入)
JWT_SECRET="change-me-to-a-long-random-secret"

# 访问令牌有效期(jose 时长写法)
JWT_TTL="2h"

# 新用户初始虚拟筹码
INITIAL_CHIPS="10000"
